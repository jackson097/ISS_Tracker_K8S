apiVersion: v1
kind: Pod
metadata:
  name: iss-tracker-pod
spec:
    containers:
    - name: iss-tracker-app
      volumeMounts:
        - name: db-mount
          mountPath: /db-mount/
      image: jacksonnewlands/iss-tracker-app:latest
    imagePullSecrets:
    - name: regcred
  
    - name: iss-tracker-db
      volumeMounts:
        - name: db-mount
          mountPath: /db-mount/
      image: jacksonnewlands/iss-tracker-db:latest
    imagePullSecrets:
    - name: regcred

    volumes:
    - name: db-mount
      emptyDir: {}